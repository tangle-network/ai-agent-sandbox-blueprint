# Adversarial Surface — ai-agent-sandbox-blueprint
Date: 2026-04-15

## Scan Dimensions (6 parallel agents)

### 1. Invariants — 25 identified
Key invariants: challenge single-use, token revocation effective (now fixed), PASETO key stability across restarts, sandbox ID immutable across recreate, TEE sandboxes not recreatable, ownership case-insensitive, circuit breaker sandbox-scoped, rate limiter XFF trust model.

### 2. Fuzz Targets — 17 identified
Critical: `verify_eip191_signature` (k256 recovery), `validate_session_token` (PASETO parsing), `build_snapshot_command` (shell injection vectors), `validate_secrets_map` (null bytes in keys), `run_port_proxy` (path traversal), `parse_sse_event` (unbounded buffer growth), `build_agent_payload` (context_json override — FIXED).

### 3. OWASP Attack Surface — 23 findings
HIGH: instance-blueprint binds 0.0.0.0 (FIXED). MEDIUM: unauthenticated provisions endpoint, S3 SSRF bypass, plaintext passthrough, verbose error messages. Correctly implemented: shell injection protection, SSRF validation for HTTPS, constant-time sidecar token comparison, CORS trust model, security headers.

### 4. Credential Findings — 3 items
WalletConnect project ID in git history (MEDIUM), dev secrets in deploy scripts (LOW, guarded), AzureConfig Debug with client_secret (LOW, no current logging).

### 5. Race Conditions — 11 findings
HIGH: sandbox count TOCTOU, secret injection race, stop/resume state machine race, store no-flock. MEDIUM: chat run cancel/complete race, session delete TOCTOU, enqueue guard doesn't span spawn. LOW: GC lock gap, reaper/GC races.

### 6. Benchmark Gaps — 12 items
No benchmark infrastructure exists. Critical hot paths: validate_session_token (every request, Mutex), RateLimiter::check (double mutex, every request), circuit_breaker::check_health (getenv on every call), PersistentStore mutations (full JSON flush to disk).
